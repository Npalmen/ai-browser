/** In-memory session for the trusted application UI renderer. */
export const APP_UI_PARTITION = 'app-ui';

/** Persistent session for future website WebContentsView tabs. */
export const WEBSITE_PARTITION = 'persist:website';
